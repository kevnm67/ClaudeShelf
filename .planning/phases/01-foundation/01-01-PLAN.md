---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Create the Xcode project with proper directory structure, build settings for Swift 6 strict concurrency, macOS 15 deployment target, and a running app shell.

Purpose: Establish the project foundation that all subsequent phases build on. Get a buildable, runnable macOS app from day one.
Output: Working Xcode project with ClaudeShelfApp entry point, placeholder ContentView, and correct build configuration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PROJECT.md

**Constraints from project instructions:**
- Swift 6 with strict concurrency checking
- macOS 15+ (Sequoia) deployment target
- SwiftUI only — no AppKit unless SwiftUI has a gap
- Zero external dependencies
- Bundle identifier: com.claudeshelf.ClaudeShelf (or similar)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project structure and build configuration</name>
  <files>
    ClaudeShelf.xcodeproj/project.pbxproj,
    ClaudeShelf/App/ClaudeShelfApp.swift,
    ClaudeShelf/App/ContentView.swift,
    ClaudeShelf/Resources/Assets.xcassets/Contents.json,
    ClaudeShelf/Resources/Assets.xcassets/AccentColor.colorset/Contents.json,
    ClaudeShelf/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json,
    ClaudeShelf/Info.plist,
    ClaudeShelf/ClaudeShelf.entitlements
  </files>
  <action>
    Create the full Xcode project structure for a macOS SwiftUI app:

    1. **Directory structure** — Create all directories per PROJECT.md architecture:
       - ClaudeShelf/App/
       - ClaudeShelf/Models/
       - ClaudeShelf/Services/
       - ClaudeShelf/Views/Sidebar/
       - ClaudeShelf/Views/Editor/
       - ClaudeShelf/Views/Cleanup/
       - ClaudeShelf/Views/Components/
       - ClaudeShelf/Utilities/
       - ClaudeShelf/Resources/
       - ClaudeShelfTests/

    2. **project.pbxproj** — Create a minimal but complete pbxproj for a macOS app target with:
       - Product name: ClaudeShelf
       - Bundle identifier: com.claudeshelf.app
       - Deployment target: macOS 15.0
       - Swift Language Version: 6
       - Strict Concurrency Checking: complete
       - SWIFT_STRICT_CONCURRENCY = complete
       - Source files: ClaudeShelfApp.swift, ContentView.swift
       - Resources: Assets.xcassets
       - Test target: ClaudeShelfTests
       - Build configurations: Debug and Release
       - Enable App Sandbox entitlement with read/write file access

    3. **ClaudeShelfApp.swift** — Minimal @main entry point:
       ```swift
       import SwiftUI

       @main
       struct ClaudeShelfApp: App {
           var body: some Scene {
               WindowGroup {
                   ContentView()
               }
           }
       }
       ```

    4. **ContentView.swift** — Placeholder three-column layout:
       ```swift
       import SwiftUI

       struct ContentView: View {
           var body: some View {
               NavigationSplitView {
                   Text("Categories")
               } content: {
                   Text("Files")
               } detail: {
                   Text("Editor")
               }
               .frame(minWidth: 800, minHeight: 500)
           }
       }
       ```

    5. **Assets.xcassets** — Standard asset catalog with AccentColor and AppIcon placeholders.

    6. **Info.plist** — Minimal Info.plist with app category (developer-tools).

    7. **Entitlements** — App Sandbox enabled with com.apple.security.files.user-selected.read-write.

    Do NOT use xcodegen or any external tool. Create the pbxproj manually — it's verbose but deterministic for a small project.
  </action>
  <verify>
    Run: `xcodebuild -project ClaudeShelf.xcodeproj -scheme ClaudeShelf -configuration Debug build 2>&1 | tail -5`
    Should show "BUILD SUCCEEDED" or similar success indicator.
  </verify>
  <done>
    Xcode project exists, builds successfully with `xcodebuild`, has Swift 6 strict concurrency enabled, targets macOS 15.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify project configuration and add .gitignore</name>
  <files>
    .gitignore
  </files>
  <action>
    1. **Verify build settings** by running xcodebuild and checking:
       - Swift version is 6
       - Deployment target is macOS 15.0
       - Strict concurrency checking is enabled
       - App runs (build succeeds)

    2. **Create .gitignore** for Xcode/Swift project:
       ```
       # Xcode
       build/
       DerivedData/
       *.xcuserdata/
       *.xcworkspace/xcuserdata/
       xcuserdata/

       # Swift Package Manager
       .build/
       Packages/

       # macOS
       .DS_Store
       *.swp
       *~

       # Xcode build
       *.moved-aside
       *.pbxuser
       *.mode1v3
       *.mode2v3
       *.perspectivev3
       !default.pbxuser
       !default.mode1v3
       !default.mode2v3
       !default.perspectivev3
       ```

    3. **Verify the app launches** by checking build output for any warnings about concurrency or deprecation.
  </action>
  <verify>
    Run: `xcodebuild -project ClaudeShelf.xcodeproj -scheme ClaudeShelf -showBuildSettings 2>/dev/null | grep -E "SWIFT_VERSION|MACOSX_DEPLOYMENT_TARGET|SWIFT_STRICT_CONCURRENCY"`
    Should show: SWIFT_VERSION = 6, MACOSX_DEPLOYMENT_TARGET = 15.0, SWIFT_STRICT_CONCURRENCY = complete
  </verify>
  <done>
    .gitignore exists, build settings verified correct, no warnings from strict concurrency checking.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project ClaudeShelf.xcodeproj -scheme ClaudeShelf build` succeeds
- [ ] Swift 6 strict concurrency enabled (no warnings suppressed)
- [ ] macOS 15.0 deployment target confirmed
- [ ] Directory structure matches PROJECT.md architecture
- [ ] .gitignore covers Xcode artifacts
</verification>

<success_criteria>

- Xcode project builds and runs (shows placeholder NavigationSplitView)
- All directory stubs created (App, Models, Services, Views/*, Utilities, Resources)
- Build settings: Swift 6, macOS 15, strict concurrency = complete
- Test target exists (ClaudeShelfTests)
- .gitignore properly excludes build artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
